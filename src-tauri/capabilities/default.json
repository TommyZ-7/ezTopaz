{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window IPC access (event subscribe for preview frames and errors)",
  "windows": ["main"],
  "permissions": ["core:event:default"]
}
